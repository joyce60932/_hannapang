# 專案記憶 — 輪播貼文製作器

## 這是什麼
@_hannapang 的 Instagram **輪播貼文製作器**（純前端網頁）。內建可切換的多套模板，
每套各自存一份內容。詳見 `README.md`。

## 使用者偏好（重要）
- ⭐ **偏好模板：「極簡白對比（模板 B）」** —— 使用者明確說喜歡這個。
  預設就用這套；除非使用者指定，新內容／示範一律以模板 B 為主。
  - 風格：白底 `#f5f4f1` + 去背產品圖 + 大數字，置中堆疊 / 左右 VS。
  - 版面固定元素：頂部標語置中、右上頁碼 `n/總數`、底部細線 + 署名 + 小字。
- **署名一律用 `@_hannapang`**，不要沿用任何參考帳號（例如 JAY 超人）的品牌。
- IP 人設：龐玉涵 / 幣圈媽媽；常用主題標籤 `#龐玉涵 #幣圈媽媽 #複利的力量`。
- 輸出尺寸：IG 直式 **1080×1350**。
- 內容常見主題：理財、機會成本、複利（生活化、易擴散）。

## 內容系列（固定欄目）
- **定期定額大富翁**：理財主軸，痛點文 ↔ 方法文交錯，Phase 1–3（見 docs/notes）。
- **成長型思維**：用書／電影／神話照出人生洞見，結尾輕扣長期/複利。
  規格見 `docs/notes/成長型思維-系列規格.md`；可重複模板 `presets/growth-template.js`。
- 出新一篇＝複製對應的 template preset，改文字＋圖，用 build-standalone 產單檔。

## 模板 A（備用）
黑金真人照（故事風）：純黑底 + 全出血真人照 + 鵝黃金 `#f6dfa3`，文字沉左下。
規格見 `docs/template-spec.md`。適合故事型／情緒型內容。

## 開發約定
- 純前端、classic script（要能用 `file://` 本機開啟），不要改成 ES modules。
- 要加新模板：在 `js/templates.js` 的 `TEMPLATES` 加物件並加進 `TEMPLATE_ORDER`；
  `app.js` / `export.js` 會自動支援。
- 改完 JS 用 `node --check` 驗證每支檔案。
- 照片：模板 A 裁 1080×1350（`cropPhoto`）；模板 B 去背 PNG 等比縮放（`fitPhoto`）。
